[project]
name = "colony-counter"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "tachoviendo", email = "ignacio.3249@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "opencv-python-headless>=5.0.0.93",
    "pandas>=3.0.6",
    "pillow>=12.3.0",
    "scipy>=1.18.1",
]

[project.scripts]
colony-counter = "colony_counter:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
